spring:
  application:
      name: Diulala
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
server:
  port: 8085

spring:
  datasource:
      url: jdbc:mysql://127.0.0.1:3306/diulala?characterEncoding=utf-8&useUnicode=true
      username: admin
      password: admin
  redis:
      host: localhost
      port: 6379
      password: admin